# Import models so they can be referenced from app.models
from app.models.users import User, GoalProgress

# This allows importing directly from app.models:
# from app.models import User, GoalProgress 
